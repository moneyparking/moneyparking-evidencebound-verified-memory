name: CI
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e '.[dev]'
      - run: ruff check . --ignore E501
      - run: mypy evidencebound --ignore-missing-imports --allow-untyped-defs
      - run: bandit -q -r evidencebound
      - run: python -m pytest -q
      - run: python -m compileall -q evidencebound tests scripts
